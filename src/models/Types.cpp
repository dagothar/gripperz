#include "Types.hpp"

using namespace gripperz::models;

std::ostream& gripperz::models::operator<<(std::ostream& stream, const GripperQuality& quality) {
    stream << "lol";
    
    return stream;
}